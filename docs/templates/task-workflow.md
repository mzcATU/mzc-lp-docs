# AI 작업 진행 규칙

> **핵심**: 코드 작성 전 **무조건 계획 먼저** → 승인 → 진행 → 완료 보고

---

## 절대 규칙

```
🚫 승인 없이 코드 작성 금지
🚫 계획 제시 없이 작업 시작 금지
🚫 "계획 보여줘" 요청 없어도 항상 계획 먼저
```

**예외**: 단순 질문, 파일 조회, 설명 요청은 바로 응답

---

## 작업 흐름 (7단계)

```
1. 요구사항 분석 → 2. UX 설계 → 3. 의존성 분석 → 4. 구현 계획
→ 5. 리스크 분석 → 6. 구현 → 7. 테스트
```

---

## 1단계: 요구사항 분석

```markdown
## 📋 작업 계획

### 요청 분석
- 요청: [요청 내용]
- 범위(Scope): [포함 항목]
- 비목표(Non-Goals): [제외 항목]
- 참조 컨벤션: [해당 파일]
```

---

## 2단계: UX 설계 (프론트엔드 작업시)

- 사용자 스토리 (AS A... I WANT... SO THAT...)
- 화면 흐름 정의
- 에러 케이스 고려

---

## 3단계: 의존성 분석

- 기존 코드 탐색 / 재사용 가능 모듈
- 외부 라이브러리 확인
- DB 스키마 변경 여부

---

## 4단계: 구현 계획 (MoSCoW 우선순위)

```markdown
### 작업 목록
| # | 작업 | 파일 | 우선순위 |
|---|------|------|:--------:|
| 1 | Entity 생성 | `User.java` | Must |
| 2 | Repository 생성 | `UserRepository.java` | Must |

---
**이 계획대로 진행할까요?**
```

### MoSCoW 우선순위

| 레벨 | 의미 | AI 지시 |
|------|------|--------|
| 🔴 **Must** | 필수 (없으면 릴리스 불가) | 반드시 구현 |
| 🟡 **Should** | 권장 (중요하지만 필수 아님) | 가능하면 구현 |
| 🟢 **Could** | 선택 (시간 허락시) | 여유시 구현 |
| ⚪ **Won't** | 제외 (이번 버전 아님) | 절대 구현 안함 |

### 작업 순서

```
Backend:  Entity → Repository → DTO → Exception → Service → Controller
Frontend: Types → API Service → React Query → Component
```

---

## 5단계: 리스크 분석

| 리스크 | 확인 | 해결책 |
|--------|------|--------|
| N+1 쿼리 | 연관 조회 | Fetch Join |
| XSS | 사용자 입력 | 이스케이프 |
| SQL Injection | 동적 쿼리 | Parameterized |

---

## 6단계: 구현

### 승인
```
승인: "ㅇㅇ" / "응" / "진행해"
수정: "X는 빼고" → 계획 수정 후 재확인
```

### TodoWrite 사용
```json
[
  { "content": "User Entity 생성", "status": "in_progress" },
  { "content": "UserRepository 생성", "status": "pending" }
]
```

### 레이어별 체크

**Backend**
- [ ] BaseTimeEntity 상속, Setter 금지
- [ ] @Transactional(readOnly=true) 클래스 레벨
- [ ] try-catch 금지 (GlobalExceptionHandler)

**Frontend**
- [ ] any 타입 금지
- [ ] React Query로 서버 상태

---

## 7단계: 테스트 & 완료

### 테스트
- [ ] 단위/통합 테스트 작성
- [ ] 테스트 통과 (`./gradlew test`, `npm test`)
- [ ] 린터 통과

### 완료 보고

```markdown
## 📝 작업 완료

### 생성 파일
| 파일 | 설명 |
|------|------|
| `User.java` | 사용자 엔티티 |
| `UserRepository.java` | JPA Repository |

### 다음 단계 (권장)
- [ ] UserService 생성
- [ ] 테스트 작성
```

---

## 예외 처리

```markdown
### ⚠️ 문제 발생
**상황**: [설명]
**옵션**:
1. [방안 A]
2. [방안 B]

**어떻게 진행할까요?**
```

---

## 작업 규모별

| 규모 | 파일 수 | 진행 방식 |
|------|:----:|---------|
| 소규모 | 1-3 | 간략 계획 → 작업 |
| 중규모 | 4-10 | 상세 계획 + TodoWrite |
| 대규모 | 10+ | 단계별 나눠서 진행 |

---

> [CLAUDE.md](../CLAUDE.md) | [moscow-priority.md](./moscow-priority.md) | [prd.md](./prd.md)
